name: SOMADEX GameState Smoke

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  gamestate-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    container: barichello/godot-ci:4.4.1
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Import project
        run: godot --headless --path . --editor --quit-after 5

      - name: Run persistent state roundtrip
        run: timeout 30s godot --headless --path . --script res://tests/game_state_smoke.gd
